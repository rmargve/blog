import { Link } from "gatsby"
import React from "react"

import "../styles/style.css"

export default () => (
  <nav className="text-teal-500 inline-block font-bold text-3xl mb-8 lg:m-12">
    <Link to="/">Midnight</Link>
  </nav>
)
